import '../../../../../../data/remote/add_unit/model/unit.dart';

class UnitsWithSelectedIdAndType {
  List<Unit> units;
  String selectedId;
  String type;
  UnitsWithSelectedIdAndType(this.units, this.selectedId, this.type);
}
